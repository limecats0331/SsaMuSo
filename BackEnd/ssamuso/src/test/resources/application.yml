#  ?autoReconnect=true&allowPublicKeyRetrieval=true&useSSL=false#
spring:
  datasource:
    url: jdbc:mariadb://limecats.synology.me:3307/SsaMuSo
    username: ssafy
    password: ssafy
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
  logging:
    level:
      org.hibernate.SQL: debug


cloud:
  aws:
    credentials:
      accessKey: AKIAUX4ZADFPUKVDE4TY
      secretKey: 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
    s3:
      bucket: prj-novel
    region:
      static: ap-northeast-2
    stack:
      auto: false

logging:
  level:
    root: info
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error